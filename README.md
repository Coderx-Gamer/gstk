# GSTK (GIS Toolkit)

---

GSTK is a Java CLI tool for downloading tiles from GIS servers onto
a local database which can be hosted locally.

## Updating

---

When updating, remember to update the pom.xml version and
PROJECT_VERSION in `org.gstk.Constants`.